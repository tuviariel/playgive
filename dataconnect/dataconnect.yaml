specVersion: "v1beta"
serviceId: "playgive"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "playgive-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
